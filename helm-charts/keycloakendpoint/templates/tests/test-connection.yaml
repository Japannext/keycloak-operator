apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloakendpoint.fullname" . }}-test-connection"
  labels:
    {{- include "keycloakendpoint.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloakendpoint.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
